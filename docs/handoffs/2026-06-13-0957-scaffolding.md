# Handoff: 2026-06-13-0957 Project scaffolding

## Summary

**Baseline:** 1 test, 1 passed. `pip install -e .[dev]` clean. `book-munger --help` works. Branch `2-a-project-scaffolding` merged to main.

---

## What happened

Triage and full implementation of issue #2 (project scaffolding) in a single session.

**Triage:** Issue #2 was assessed as `roadmap / ready-for-agent`. A research spike confirmed `wordfreq` 3.1.1 installs cleanly on Python 3.12 despite being in sunset mode. An agent brief was posted on the issue flagging the sunset status and deferring the `wordfreq` vs NLTK FreqDist decision to a later issue.

**Environment:** A `.venv` was created at the repo root using `python3 -m venv`. Instructions were added to `AGENTS.md` (the target of the `CLAUDE.md` symlink) so future sessions use the local environment exclusively. `pip install -e .[dev]` is the install command.

**Scaffolding (TDD):** Bootstrap phase (no pytest yet) created `pyproject.toml` and the `src/book_munger/__init__.py` stub. After install, a failing test `test_help_exits_cleanly` was written against `book_munger.cli:cli` (RED), then the Click group stub was implemented to pass it (GREEN). No refactor needed.

**Build backend note:** `setuptools.backends.legacy:build` was rejected — pip's isolated build environment uses the system Python which lacks that module. `setuptools.build_meta` is used instead.

## Artefacts

- Commit: `8ba7c04` on branch `2-a-project-scaffolding` (merged)
- Agent brief: https://github.com/DC23/book-munger/issues/2#issuecomment-4696423872
- ADR 0001: [src layout](../adr/0001-src-layout.md)
- ADR 0002: [Click for CLI](../adr/0002-click-for-cli.md)

## Deferred decisions

- `wordfreq` (sunset mode, data frozen ~2021) vs NLTK `FreqDist` on the source corpus. A `# TODO: evaluate wordfreq vs NLTK FreqDist` comment is requested at the first import site when wordfreq is used in a future issue. The agent brief on #2 captures this.
