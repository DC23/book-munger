# Test Fixtures

Small, self-contained sample files used by the test suite. Each fixture should be the minimum size needed to exercise the behaviour under test.

Naming convention: `<feature>_<description>.<ext>` — e.g. `frequency_short_novel.txt`, `template_basic.jinja2`.
